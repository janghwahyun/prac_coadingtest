// 소수 자기 자신과 1로 나누어지는 숫자
// using nested loop


const start = '3';
const end = '16';
const arr = [];

for (let i = 2; i <= end; i++) {
	arr.push(true);
}
	
	
	if (primeN[i] / i === primeN[i] && primeN[i] / i === 1) {
    arr.push(i);
  } else {
    returnl;
  }
}

console.log(arr);